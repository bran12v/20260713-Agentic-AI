services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: support
      POSTGRES_PASSWORD: support_dev
      POSTGRES_DB: support
    ports:
      - "5432:5432"
    volumes:
      - support_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U support -d support"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s

  web:
    build:
      context:  ..
      dockerfile: docker/Dockerfile

    ports:
      - "5000:5000"
    
    environment:
      LOG_LEVEL: info
      LOG_JSON: "1"
      DATABASE_URL: postgresql://support:support_dev@db:5432/support
      DATA_DIR: /app/data

    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:5000/ready').raise_for_status()"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s

    command: >
      sh -c "
        python -c 'from support_api.storage import init_db, seed_from_json; init_db(); seed_from_json()' && gunicorn --bind 0.0.0.0:5000 --workers 2 --access-logfile - --error-logfile - 'support_api.api:create_app()'
      "

    depends_on:
      db:
        condition: service_healthy
    
volumes:
  support_pgdata: